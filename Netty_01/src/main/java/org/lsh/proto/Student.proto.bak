syntax = "proto3"; // 协议的版本
option java_outer_classname = "StudentPOJO"; //生成的外部类名,同时也是文件名
// protobuf 是以message数据

// 会在StudentPOJO外部类生成一个内部类Student，他是真正发送POJO对象
message Student{
  int32 id = 1; // Student类中有一个属性名字为id 类型为int32(protobuf类型) 1为属性序号不是值
  string name = 2;
}
